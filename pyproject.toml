[project]
name = "symphony-oc"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "jinja2>=3.0",
    "pyyaml>=6.0",
    "json5>=0.9",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
